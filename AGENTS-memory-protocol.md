# AGENTS memory protocol template

この文書は、個人用・ローカル運用の `codex-memory-sidecar` を Codex/AGENTS 系の作業手順に組み込むためのテンプレートです。必要に応じてプロジェクトごとの `AGENTS.md` に貼り付けるか、このファイルを参照してください。

## 基本方針

- メモリは「作業を速くする補助情報」であり、現在のリポジトリ、ユーザー指示、実ファイルの内容より優先しない。
- 参照・書き込みはローカル MCP ツール経由で行う。外部サービスへ自動送信しない。
- 非自明な作業では最初に `memory_digest(taskDescription, projectPath)` を呼び、関連しそうな背景、過去の判断、注意点を確認する。
- 設計判断、仕様解釈、既存方針の確認が必要なときは `search_memory` で関連する `core` / `recall` メモリを探す。
- 作業完了後、次回以降の助けになる知見がある場合だけ `write_memory` または `update_memory` を検討する。

## 使うとき

- 変更が複数ファイル、設計判断、運用手順、MCP ツール仕様、個人設定に関わる。
- 以前の実装意図、過去の失敗、ローカル環境の癖を確認したい。
- ユーザーが「前に決めたこと」「いつもの方針」「この環境の設定」に触れている。
- 作業後に、再利用できる決定、検証結果、ポストモーテム、注意点が残った。

## 使わないとき

- 単純な typo 修正、明確な 1 ファイル変更、短い質問への回答など、メモリ参照の価値が低い。
- リポジトリ内の実ファイルを読めば十分に判断できる。
- ユーザーがメモリを使わないよう明示した。
- 検索語を変えて何度も探索しても新しい判断材料が増えない。過剰呼び出しは避ける。

## 作業前プロトコル

1. タスクが非自明なら `memory_digest` を呼ぶ。
   - `taskDescription`: 今回の作業を 1 文で具体的に書く。
   - `projectPath`: 対象リポジトリの絶対パスを渡す。
2. 返ってきた内容を、現在のユーザー指示と実ファイルで検証する。
3. 設計・方針に関する判断が残る場合は `search_memory` で関連メモリを絞り込む。
4. メモリが古い、曖昧、矛盾している場合は、推測として扱い、必要ならユーザーに確認する。

## 作業後プロトコル

- 次回も使う価値がある情報だけ保存する。
- 保存候補:
  - プロジェクト固有の運用ルール
  - 繰り返し発生した不具合と原因
  - ユーザーが明示した好みや制約
  - MCP ツールの挙動、バックアップ、監査に関する確認結果
- 保存しない候補:
  - 一時的な作業ログ
  - すぐ失効する状態
  - 秘密情報、トークン、個人情報の詳細
  - 実ファイルや git 履歴で十分に追跡できる内容
- 既存メモリの更新で足りる場合は、新規作成より `update_memory` を優先する。
- 失敗から学んだことがある場合は、短いポストモーテムとして原因、影響、次回の回避策を残す。

## 引用と監査

- メモリ由来の主張をユーザーに伝えるときは、可能な限り memory id を添える。
- memory id がない要約を使う場合は、「メモリ要約によると」のように出所を明示する。
- 重要な判断は `audit_memory` で最近の参照・書き込みイベントを確認できる形にする。
- `search_memory` の結果を根拠にした場合は、該当 id、検索意図、現在のファイル確認結果を区別する。
- メモリと実ファイルが矛盾した場合は、実ファイルとユーザーの最新指示を優先し、必要に応じて古いメモリを更新する。

## 安全性とプライバシー

- このプロジェクトは個人用ローカルメモリを前提とする。保存先はローカル SQLite DB とし、外部共有を前提にしない。
- 秘密情報、認証情報、個人情報の詳細、削除すべきデータは保存しない。
- バックアップが必要な場合は `backup_memory` を使い、必要に応じて `verify_backup` / `inspect_backup` で確認する。
- メモリの削除や忘却が必要な場合は、安全な削除方針を確認し、破壊的操作は明示確認のうえで行う。
- dashboard や監査情報を扱うときも、本文や payload に機微情報が含まれ得る前提で扱う。

## 最小テンプレート

```md
## Memory protocol

- 非自明な作業の前に `memory_digest(taskDescription, projectPath)` を呼ぶ。
- 設計判断や過去方針の確認が必要なときは `search_memory` で関連する `core` / `recall` メモリを探す。
- メモリ由来の主張は memory id などで出所を監査可能にする。
- 作業完了後、次回に有用な知見だけ `write_memory` / `update_memory` で保存を検討する。
- 単純作業では過剰に呼び出さない。秘密情報や個人情報の詳細は保存しない。
```
