interface:
  display_name: "Codex Memory Sidecar"
  short_description: "Route memory-aware Codex work through local session context"
  default_prompt: "Use Codex Memory Sidecar to load local session context, directives, and recent memory before continuing this task."
dependencies:
  tools:
    - type: "mcp"
      value: "codex-memory-sidecar"
      description: "Local Codex Memory Sidecar MCP server"
